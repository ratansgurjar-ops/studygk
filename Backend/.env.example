PORT=4000
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_password_here
DB_DATABASE=studygk
# Some deployments (older setups) use the env var name `DB_NAME` instead of `DB_DATABASE`.
# The server will accept either `DB_NAME` or `DB_DATABASE` for MySQL connection.
# Example: DB_NAME=studygk
JWT_SECRET=replace_with_strong_secret_in_prod
